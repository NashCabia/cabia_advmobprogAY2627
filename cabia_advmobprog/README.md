# Lab Activity 2 Discussion

This lab activity demonstrates a Flutter app that uses a simple model-service-screen architecture.
The model defines the product data, the service provides sample data, and the screen renders the list and detail views.
A Provider-based theme manager is also used to switch between light and dark mode across the app.

The new design pattern improves code organization by separating data, UI, and state management into dedicated folders and files.
